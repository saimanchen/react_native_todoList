import React from 'react';
import { View, Text } from 'react-native';

export const Header = () => (
  <View style={globalStyles.header}>
    <Text style={globalStyles.text}>TODO LIST</Text>
  </View>
);